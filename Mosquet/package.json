{
  "name": "design-hub",
  "version": "36.3.0",
  "private": true,
  "description": "Design Hub V36.3 — workspace colaborativo",
  "scripts": {
    "check": "node --check app.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
