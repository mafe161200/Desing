{
  "name": "design-hub",
  "version": "36.5.0",
  "private": true,
  "description": "Design Hub V36.5 — workspace colaborativo",
  "scripts": {
    "check": "node --check app.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
